<?php

declare(strict_types=1);

namespace CasesBot\Bot;

use CasesBot\Catalog\CatalogRepository;
use CasesBot\Presentation\PresentationBuilder;
use CasesBot\Query\Matcher;
use CasesBot\Query\QueryParser;

/**
 * Обрабатывает входящее сообщение: QueryParser -> Matcher -> PresentationBuilder -> ответ (§6.1, §6.2 ТЗ).
 */
class ChatBotController
{
}
